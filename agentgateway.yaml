llm:
  port: 4000
  models:
    - name: "claude-*"
      provider: anthropic
      params:
        apiKey: "$ANTHROPIC_API_KEY"
    - name: "*"
      provider: openAI
      params:
        hostOverride: "host.docker.internal:11434"
