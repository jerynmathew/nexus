binds:
  - port: 4000
    listeners:
      - routes:
          - name: anthropic
            backends:
              - ai:
                  name: anthropic
                  provider:
                    anthropic:
                      model: claude-sonnet-4-20250514
            policies:
              backendAuth:
                key: "$ANTHROPIC_API_KEY"
