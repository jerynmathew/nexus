binds:
  - port: 4000
    listeners:
      - routes:
          - name: anthropic
            backends:
              - ai:
                  name: anthropic
                  provider:
                    anthropic: {}
            policies:
              backendAuth:
                key: "$ANTHROPIC_API_KEY"
